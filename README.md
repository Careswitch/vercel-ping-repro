# vercel-ping-repro

```bash
curl -gkvLI -b '' -H 'User-Agent: updown.io daemon 2.9' -H 'Accept: */*' -H 'Connection: Close' -H 'Accept-Language: en' -H 'Accept-Encoding: deflate, gzip, br, zstd' -m 30 --connect-timeout 10 --compressed http://localhost:3001/health
```
